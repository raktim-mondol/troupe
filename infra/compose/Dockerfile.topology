FROM node:22-bookworm

RUN corepack enable
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @troupe/db generate
RUN install -d -m 750 -o node -g node /data

ENV NODE_ENV=production
EXPOSE 3100 7091
USER node
CMD ["pnpm", "--filter", "@troupe/api", "start"]
